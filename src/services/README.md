# Source services

Create one adapter directory per verified official/authorized source. External endpoints and licenses must never be guessed. Integrations remain disabled until the data-source registry is approved.
