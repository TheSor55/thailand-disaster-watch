# Variables prefixed with VITE_ are public and must never contain secrets.
APP_ENV=development
VITE_PUBLIC_APP_ORIGIN=http://localhost:3000
VITE_API_BASE_URL=http://localhost:8787

# Server-side placeholders only. Keep values empty until the source and license
# are approved and configure real values through secure Worker bindings.
GISTDA_API_KEY=
GISTDA_PILOT_ENABLED=false
GISTDA_REQUEST_TIMEOUT_MS=
TMD_API_KEY=
CLOUDFLARE_ACCOUNT_ID=
